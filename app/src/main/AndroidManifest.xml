<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.runningforlife.photofans">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:name=".app.AppGlobals"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.GalleryActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.ImageDetailActivity"
            android:theme="@style/AppTheme.NoActionBar.DarkStatusBar"
            android:label=""
            android:parentActivityName=".ui.activity.GalleryActivity">

        </activity>

        <activity android:name=".ui.activity.SettingsActivity"
            android:theme="@style/AppTheme"
            android:label="@string/action_settings">

        </activity>

        <activity android:name=".ui.activity.ImageSourceSelectionActivity"
            android:theme="@style/AppTheme"
            android:label="@string/select_your_favorite_websites">
        </activity>

        <service android:name=".service.ImageRetrieveService">
        </service>

    </application>

</manifest>